# stun_test


```
chmod +x scripts/build.sh scripts/run.sh

# build 两个镜像
./scripts/build.sh

# 起 server
./scripts/run.sh up

# 跑 client 测试
./scripts/run.sh test

# 看 server 日志
./scripts/run.sh logs

# 停掉并清理 network
./scripts/run.sh down

```
